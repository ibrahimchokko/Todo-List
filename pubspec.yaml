name: todo_list
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  vibration: ^3.1.3
  # audioplayers removed to avoid Linux system dependency on GStreamer
  # If you need audio later, re-add and ensure the required system
  # packages (gstreamer dev) are installed on Linux.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
